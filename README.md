# my_vimrc

All the plugins are managed by Vundle. So, first of all, you should do this:

    git clone https:github.comVundleVimVundle.vim.git ~.vimbundleVundle.vim

Next step, use `:PluginInstall` while open this `.vimrc'.

All done!
